pytest>=9.1.1
numpy>=2.5.3
# tests/test_event_provider.py and tests/test_event_worker.py use @pytest.mark.asyncio;
# without this, pytest silently reports every async test as a failure with
# "async def functions are not natively supported" instead of running it.
pytest-asyncio==1.4.0
