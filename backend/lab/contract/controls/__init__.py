"""Deliberately defective implementations.

Every module here is a CONTROL. Controls exist so the evaluator can be shown to
reject what it claims to reject; they are never presented as candidate work and
their ids are prefixed `control:` everywhere they appear.
"""
