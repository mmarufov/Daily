"""Daily Lab: a controlled experiment on Daily's batch relevance scorer.

The package is deliberately dependency-free (standard library only). Candidate
code runs with no third-party packages available, so "pinned dependencies" is
the strongest possible statement: there are none to pin.
"""
